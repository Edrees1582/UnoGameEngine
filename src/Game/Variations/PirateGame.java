package Game.Variations;

import Game.GameRules;

public class PirateGame extends StandardGame {
    public PirateGame(int numberOfPlayers, int initTurn, int initDir, GameRules gameRules) {
        super(numberOfPlayers, initTurn, initDir, gameRules);
    }
}
